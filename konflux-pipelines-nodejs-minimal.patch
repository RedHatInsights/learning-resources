## Changes needed for catastrophe-brandon/konflux-pipelines branch btweed/nodejs-minimal

In the file: pipelines/platform-ui/docker-build-run-all-tests.yaml

Replace BOTH occurrences of:
  - image: registry.access.redhat.com/ubi8/nodejs-22

With:
  - image: registry.redhat.io/ubi9/nodejs-22-minimal:latest

---

There are 2 occurrences to replace:

1. In the "setup-workspace" task (around line 140)
2. In the "run-unit-tests" task (around line 165)

---

Example diff for both locations:

```diff
       - name: use-trusted-artifact
         image: quay.io/redhat-appstudio/build-trusted-artifacts:latest@sha256:9b180776a41d9a22a1c51539f1647c60defbbd55b44bbebdd4130e33512d8b0d
         args:
         - use
         - $(params.SOURCE_ARTIFACT)=/var/workdir
-      - image: registry.access.redhat.com/ubi8/nodejs-22
+      - image: registry.redhat.io/ubi9/nodejs-22-minimal:latest
         workingDir: /var/workdir
         name: setup-workspace
         securityContext:
           runAsUser: 0
         script: $(params.workspace-setup-script)
```

And:

```diff
       - name: use-trusted-artifact
         image: quay.io/redhat-appstudio/build-trusted-artifacts:latest@sha256:9b180776a41d9a22a1c51539f1647c60defbbd55b44bbebdd4130e33512d8b0d
         args:
         - use
         - $(params.SOURCE_ARTIFACT)=/var/workdir
-      - image: registry.access.redhat.com/ubi8/nodejs-22
+      - image: registry.redhat.io/ubi9/nodejs-22-minimal:latest
         workingDir: /var/workdir
         name: unit-tests
         securityContext:
           runAsUser: 0
         script: $(params.unit-tests-script)
```

---

Steps to apply:
1. Clone/navigate to catastrophe-brandon/konflux-pipelines
2. Create branch: git checkout -b btweed/nodejs-minimal
3. Edit pipelines/platform-ui/docker-build-run-all-tests.yaml
4. Replace both occurrences as shown above
5. Commit and push to catastrophe-brandon/konflux-pipelines
